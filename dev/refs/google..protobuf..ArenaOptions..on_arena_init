<use f='include/google/protobuf/arena.h' l='148' u='w' c='_ZN6google8protobuf12ArenaOptionsC1Ev'/>
<dec f='include/google/protobuf/arena.h' l='163' type='void *(*)(google::protobuf::Arena *)'/>
<use f='include/google/protobuf/arena.h' l='276' u='r' c='_ZN6google8protobuf5Arena4InitERKNS0_12ArenaOptionsE'/>
<use f='include/google/protobuf/arena.h' l='277' u='r' c='_ZN6google8protobuf5Arena4InitERKNS0_12ArenaOptionsE'/>
<offset>384</offset>
<doc f='include/google/protobuf/arena.h' l='154'>// Hooks for adding external functionality such as user-specific metrics
  // collection, specific debugging abilities, etc.
  // Init hook may return a pointer to a cookie to be stored in the arena.
  // reset and destruction hooks will then be called with the same cookie
  // pointer. This allows us to save an external object per arena instance and
  // use it on the other hooks (Note: It is just as legal for init to return
  // NULL and not use the cookie feature).
  // on_arena_reset and on_arena_destruction also receive the space used in
  // the arena just before the reset.</doc>
